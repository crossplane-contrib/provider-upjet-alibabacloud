---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: loadbalancers.nlb.alibabacloud.crossplane.io
spec:
  group: nlb.alibabacloud.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - alibabacloud
    kind: LoadBalancer
    listKind: LoadBalancerList
    plural: loadbalancers
    singular: loadbalancer
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: LoadBalancer is the Schema for the LoadBalancers API. Provides
          a Alicloud Network Load Balancer (NLB) Load Balancer resource.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LoadBalancerSpec defines the desired state of LoadBalancer
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  addressIpVersion:
                    description: 'The protocol version. Valid values:'
                    type: string
                  addressType:
                    description: 'The type of IPv4 address used by the NLB instance.
                      Valid values:'
                    type: string
                  bandwidthPackageId:
                    description: The ID of the EIP bandwidth plan that is associated
                      with the Internet-facing NLB instance.
                    type: string
                  cps:
                    description: 'The speed limit of new connections per second processed
                      by NLB instances in each VIP. Value range: 0 to 1000000.'
                    type: number
                  crossZoneEnabled:
                    description: 'Specifies whether to enable cross-zone load balancing
                      for the NLB instance. Valid values:'
                    type: boolean
                  deletionProtectionConfig:
                    description: 'Specifies whether to enable deletion protection.
                      Default value: false. See deletion_protection_config below.'
                    items:
                      properties:
                        enabled:
                          description: 'Specifies whether to enable deletion protection.
                            Valid values:'
                          type: boolean
                        reason:
                          description: The reason why deletion protection is enabled.
                            The reason must be 2 to 128 characters in length, and
                            can contain letters, digits, periods (.), underscores
                            (_), and hyphens (-). The reason must start with a letter.
                          type: string
                      type: object
                    type: array
                  deletionProtectionEnabled:
                    description: 'Specifies whether to enable deletion protection.
                      Default value: false. Valid values:'
                    type: boolean
                  deletionProtectionReason:
                    description: The reason why the deletion protection feature is
                      enabled or disabled. The deletion_protection_reason takes effect
                      only when deletion_protection_enabled is set to true.
                    type: string
                  ipv6AddressType:
                    description: 'The type of IPv6 address used by the NLB instance.
                      Valid values:'
                    type: string
                  loadBalancerName:
                    description: |-
                      The name of the NLB instance.
                      The value must be 2 to 128 characters in length, and can contain letters, digits, periods (.), underscores (_), and hyphens (-). The value must start with a letter.
                    type: string
                  loadBalancerType:
                    description: The type of the Server Load Balancer (SLB) instance.
                      Set the value to network, which specifies NLB.
                    type: string
                  modificationProtectionConfig:
                    description: 'Specifies whether to enable the configuration read-only
                      mode. Default value: NonProtection. See modification_protection_config
                      below.'
                    items:
                      properties:
                        reason:
                          description: The reason why the configuration read-only
                            mode is enabled. The value must be 2 to 128 characters
                            in length, and can contain letters, digits, periods (.),
                            underscores (_), and hyphens (-). The value must start
                            with a letter.
                          type: string
                        status:
                          description: 'Specifies whether to enable the configuration
                            read-only mode. Valid values:'
                          type: string
                      type: object
                    type: array
                  modificationProtectionReason:
                    description: The reason why the configuration read-only mode is
                      enabled. The modification_protection_reason takes effect only
                      when modification_protection_status is set to ConsoleProtection.
                    type: string
                  modificationProtectionStatus:
                    description: 'Specifies whether to enable the configuration read-only
                      mode. Default value: NonProtection. Valid values:'
                    type: string
                  paymentType:
                    description: The payment type of the resource
                    type: string
                  region:
                    description: Region is the region you'd like your resource to
                      be created in.
                    type: string
                  resourceGroupId:
                    description: |-
                      The ID of the new resource group.
                      You can log on to the Resource Management console to view resource group IDs.
                    type: string
                  securityGroupIdRefs:
                    description: References to SecurityGroup in ecs to populate securityGroupIds.
                    items:
                      description: A Reference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  securityGroupIdSelector:
                    description: Selector for a list of SecurityGroup in ecs to populate
                      securityGroupIds.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  securityGroupIds:
                    description: The security group to which the network-based SLB
                      instance belongs.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  vpcId:
                    description: The ID of the VPC where the NLB instance is deployed.
                    type: string
                  vpcIdRef:
                    description: Reference to a VPC in vpc to populate vpcId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  vpcIdSelector:
                    description: Selector for a VPC in vpc to populate vpcId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  zoneMappings:
                    description: Available Area Configuration List. You must add at
                      least two zones. You can add a maximum of 10 zones. See zone_mappings
                      below.
                    items:
                      properties:
                        allocationId:
                          description: The ID of the elastic IP address (EIP) that
                            is associated with the Internet-facing NLB instance. You
                            can specify one EIP for each zone. You must add at least
                            two zones. You can add a maximum of 10 zones.
                          type: string
                        ipv4LocalAddresses:
                          description: IPv4 Local address list. The list of addresses
                            that NLB interacts with backend services.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        ipv6Address:
                          description: The IPv6 address of the NLB instance.
                          type: string
                        ipv6LocalAddresses:
                          description: IPv6 Local address list. The list of addresses
                            that NLB interacts with backend services.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        privateIpv4Address:
                          description: The private IP address. You must add at least
                            two zones. You can add a maximum of 10 zones.
                          type: string
                        vswitchId:
                          description: The vSwitch in the zone. You can specify only
                            one vSwitch (subnet) in each zone of an NLB instance.
                            You must add at least two zones. You can add a maximum
                            of 10 zones.
                          type: string
                        vswitchIdRef:
                          description: Reference to a Vswitch in vpc to populate vswitchId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        vswitchIdSelector:
                          description: Selector for a Vswitch in vpc to populate vswitchId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        zoneId:
                          description: |-
                            The ID of the zone of the NLB instance. You must add at least two zones. You can add a maximum of 10 zones.
                            You can call the DescribeZones operation to query the most recent zone list.
                          type: string
                        zoneIdRef:
                          description: Reference to a Vswitch in vpc to populate zoneId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        zoneIdSelector:
                          description: Selector for a Vswitch in vpc to populate zoneId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  addressIpVersion:
                    description: 'The protocol version. Valid values:'
                    type: string
                  addressType:
                    description: 'The type of IPv4 address used by the NLB instance.
                      Valid values:'
                    type: string
                  bandwidthPackageId:
                    description: The ID of the EIP bandwidth plan that is associated
                      with the Internet-facing NLB instance.
                    type: string
                  cps:
                    description: 'The speed limit of new connections per second processed
                      by NLB instances in each VIP. Value range: 0 to 1000000.'
                    type: number
                  crossZoneEnabled:
                    description: 'Specifies whether to enable cross-zone load balancing
                      for the NLB instance. Valid values:'
                    type: boolean
                  deletionProtectionConfig:
                    description: 'Specifies whether to enable deletion protection.
                      Default value: false. See deletion_protection_config below.'
                    items:
                      properties:
                        enabled:
                          description: 'Specifies whether to enable deletion protection.
                            Valid values:'
                          type: boolean
                        reason:
                          description: The reason why deletion protection is enabled.
                            The reason must be 2 to 128 characters in length, and
                            can contain letters, digits, periods (.), underscores
                            (_), and hyphens (-). The reason must start with a letter.
                          type: string
                      type: object
                    type: array
                  deletionProtectionEnabled:
                    description: 'Specifies whether to enable deletion protection.
                      Default value: false. Valid values:'
                    type: boolean
                  deletionProtectionReason:
                    description: The reason why the deletion protection feature is
                      enabled or disabled. The deletion_protection_reason takes effect
                      only when deletion_protection_enabled is set to true.
                    type: string
                  ipv6AddressType:
                    description: 'The type of IPv6 address used by the NLB instance.
                      Valid values:'
                    type: string
                  loadBalancerName:
                    description: |-
                      The name of the NLB instance.
                      The value must be 2 to 128 characters in length, and can contain letters, digits, periods (.), underscores (_), and hyphens (-). The value must start with a letter.
                    type: string
                  loadBalancerType:
                    description: The type of the Server Load Balancer (SLB) instance.
                      Set the value to network, which specifies NLB.
                    type: string
                  modificationProtectionConfig:
                    description: 'Specifies whether to enable the configuration read-only
                      mode. Default value: NonProtection. See modification_protection_config
                      below.'
                    items:
                      properties:
                        reason:
                          description: The reason why the configuration read-only
                            mode is enabled. The value must be 2 to 128 characters
                            in length, and can contain letters, digits, periods (.),
                            underscores (_), and hyphens (-). The value must start
                            with a letter.
                          type: string
                        status:
                          description: 'Specifies whether to enable the configuration
                            read-only mode. Valid values:'
                          type: string
                      type: object
                    type: array
                  modificationProtectionReason:
                    description: The reason why the configuration read-only mode is
                      enabled. The modification_protection_reason takes effect only
                      when modification_protection_status is set to ConsoleProtection.
                    type: string
                  modificationProtectionStatus:
                    description: 'Specifies whether to enable the configuration read-only
                      mode. Default value: NonProtection. Valid values:'
                    type: string
                  paymentType:
                    description: The payment type of the resource
                    type: string
                  resourceGroupId:
                    description: |-
                      The ID of the new resource group.
                      You can log on to the Resource Management console to view resource group IDs.
                    type: string
                  securityGroupIdRefs:
                    description: References to SecurityGroup in ecs to populate securityGroupIds.
                    items:
                      description: A Reference to a named object.
                      properties:
                        name:
                          description: Name of the referenced object.
                          type: string
                        policy:
                          description: Policies for referencing.
                          properties:
                            resolution:
                              default: Required
                              description: |-
                                Resolution specifies whether resolution of this reference is required.
                                The default is 'Required', which means the reconcile will fail if the
                                reference cannot be resolved. 'Optional' means this reference will be
                                a no-op if it cannot be resolved.
                              enum:
                              - Required
                              - Optional
                              type: string
                            resolve:
                              description: |-
                                Resolve specifies when this reference should be resolved. The default
                                is 'IfNotPresent', which will attempt to resolve the reference only when
                                the corresponding field is not present. Use 'Always' to resolve the
                                reference on every reconcile.
                              enum:
                              - Always
                              - IfNotPresent
                              type: string
                          type: object
                      required:
                      - name
                      type: object
                    type: array
                  securityGroupIdSelector:
                    description: Selector for a list of SecurityGroup in ecs to populate
                      securityGroupIds.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  securityGroupIds:
                    description: The security group to which the network-based SLB
                      instance belongs.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  vpcId:
                    description: The ID of the VPC where the NLB instance is deployed.
                    type: string
                  vpcIdRef:
                    description: Reference to a VPC in vpc to populate vpcId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  vpcIdSelector:
                    description: Selector for a VPC in vpc to populate vpcId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  zoneMappings:
                    description: Available Area Configuration List. You must add at
                      least two zones. You can add a maximum of 10 zones. See zone_mappings
                      below.
                    items:
                      properties:
                        allocationId:
                          description: The ID of the elastic IP address (EIP) that
                            is associated with the Internet-facing NLB instance. You
                            can specify one EIP for each zone. You must add at least
                            two zones. You can add a maximum of 10 zones.
                          type: string
                        ipv4LocalAddresses:
                          description: IPv4 Local address list. The list of addresses
                            that NLB interacts with backend services.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        ipv6Address:
                          description: The IPv6 address of the NLB instance.
                          type: string
                        ipv6LocalAddresses:
                          description: IPv6 Local address list. The list of addresses
                            that NLB interacts with backend services.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        privateIpv4Address:
                          description: The private IP address. You must add at least
                            two zones. You can add a maximum of 10 zones.
                          type: string
                        vswitchId:
                          description: The vSwitch in the zone. You can specify only
                            one vSwitch (subnet) in each zone of an NLB instance.
                            You must add at least two zones. You can add a maximum
                            of 10 zones.
                          type: string
                        vswitchIdRef:
                          description: Reference to a Vswitch in vpc to populate vswitchId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        vswitchIdSelector:
                          description: Selector for a Vswitch in vpc to populate vswitchId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                        zoneId:
                          description: |-
                            The ID of the zone of the NLB instance. You must add at least two zones. You can add a maximum of 10 zones.
                            You can call the DescribeZones operation to query the most recent zone list.
                          type: string
                        zoneIdRef:
                          description: Reference to a Vswitch in vpc to populate zoneId.
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        zoneIdSelector:
                          description: Selector for a Vswitch in vpc to populate zoneId.
                          properties:
                            matchControllerRef:
                              description: |-
                                MatchControllerRef ensures an object with the same controller reference
                                as the selecting object is selected.
                              type: boolean
                            matchLabels:
                              additionalProperties:
                                type: string
                              description: MatchLabels ensures an object with matching
                                labels is selected.
                              type: object
                            policy:
                              description: Policies for selection.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          type: object
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: |-
                  PublishConnectionDetailsTo specifies the connection secret config which
                  contains a name, metadata and a reference to secret store config to
                  which any connection details for this managed resource should be written.
                  Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: |-
                      SecretStoreConfigRef specifies which secret store config should be used
                      for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          Annotations are the annotations to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.annotations".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          Labels are the labels/tags to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      type:
                        description: |-
                          Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                  This field is planned to be replaced in a future release in favor of
                  PublishConnectionDetailsTo. Currently, both could be set independently
                  and connection details would be published to both without affecting
                  each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.addressType is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.addressType)
                || (has(self.initProvider) && has(self.initProvider.addressType))'
            - message: spec.forProvider.zoneMappings is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.zoneMappings)
                || (has(self.initProvider) && has(self.initProvider.zoneMappings))'
          status:
            description: LoadBalancerStatus defines the observed state of LoadBalancer.
            properties:
              atProvider:
                properties:
                  addressIpVersion:
                    description: 'The protocol version. Valid values:'
                    type: string
                  addressType:
                    description: 'The type of IPv4 address used by the NLB instance.
                      Valid values:'
                    type: string
                  bandwidthPackageId:
                    description: The ID of the EIP bandwidth plan that is associated
                      with the Internet-facing NLB instance.
                    type: string
                  cps:
                    description: 'The speed limit of new connections per second processed
                      by NLB instances in each VIP. Value range: 0 to 1000000.'
                    type: number
                  createTime:
                    description: Resource creation time, using Greenwich Mean Time,
                      formating' yyyy-MM-ddTHH:mm:ssZ '.
                    type: string
                  crossZoneEnabled:
                    description: 'Specifies whether to enable cross-zone load balancing
                      for the NLB instance. Valid values:'
                    type: boolean
                  deletionProtectionConfig:
                    description: 'Specifies whether to enable deletion protection.
                      Default value: false. See deletion_protection_config below.'
                    items:
                      properties:
                        enabled:
                          description: 'Specifies whether to enable deletion protection.
                            Valid values:'
                          type: boolean
                        enabledTime:
                          description: Opening time of enable deletion protection.
                          type: string
                        reason:
                          description: The reason why deletion protection is enabled.
                            The reason must be 2 to 128 characters in length, and
                            can contain letters, digits, periods (.), underscores
                            (_), and hyphens (-). The reason must start with a letter.
                          type: string
                      type: object
                    type: array
                  deletionProtectionEnabled:
                    description: 'Specifies whether to enable deletion protection.
                      Default value: false. Valid values:'
                    type: boolean
                  deletionProtectionReason:
                    description: The reason why the deletion protection feature is
                      enabled or disabled. The deletion_protection_reason takes effect
                      only when deletion_protection_enabled is set to true.
                    type: string
                  dnsName:
                    description: The domain name of the NLB instance.
                    type: string
                  id:
                    description: The ID of the resource supplied above.
                    type: string
                  ipv6AddressType:
                    description: 'The type of IPv6 address used by the NLB instance.
                      Valid values:'
                    type: string
                  loadBalancerBusinessStatus:
                    description: The business status of the NLB instance.
                    type: string
                  loadBalancerName:
                    description: |-
                      The name of the NLB instance.
                      The value must be 2 to 128 characters in length, and can contain letters, digits, periods (.), underscores (_), and hyphens (-). The value must start with a letter.
                    type: string
                  loadBalancerType:
                    description: The type of the Server Load Balancer (SLB) instance.
                      Set the value to network, which specifies NLB.
                    type: string
                  modificationProtectionConfig:
                    description: 'Specifies whether to enable the configuration read-only
                      mode. Default value: NonProtection. See modification_protection_config
                      below.'
                    items:
                      properties:
                        enabledTime:
                          description: Opening time of enable deletion protection.
                          type: string
                        reason:
                          description: The reason why the configuration read-only
                            mode is enabled. The value must be 2 to 128 characters
                            in length, and can contain letters, digits, periods (.),
                            underscores (_), and hyphens (-). The value must start
                            with a letter.
                          type: string
                        status:
                          description: 'Specifies whether to enable the configuration
                            read-only mode. Valid values:'
                          type: string
                      type: object
                    type: array
                  modificationProtectionReason:
                    description: The reason why the configuration read-only mode is
                      enabled. The modification_protection_reason takes effect only
                      when modification_protection_status is set to ConsoleProtection.
                    type: string
                  modificationProtectionStatus:
                    description: 'Specifies whether to enable the configuration read-only
                      mode. Default value: NonProtection. Valid values:'
                    type: string
                  paymentType:
                    description: The payment type of the resource
                    type: string
                  regionId:
                    description: The ID of the region where the NLB instance is deployed.
                    type: string
                  resourceGroupId:
                    description: |-
                      The ID of the new resource group.
                      You can log on to the Resource Management console to view resource group IDs.
                    type: string
                  securityGroupIds:
                    description: The security group to which the network-based SLB
                      instance belongs.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                  status:
                    description: The status of the NLB instance.
                    type: string
                  tags:
                    additionalProperties:
                      type: string
                    description: Key-value map of resource tags.
                    type: object
                    x-kubernetes-map-type: granular
                  vpcId:
                    description: The ID of the VPC where the NLB instance is deployed.
                    type: string
                  zoneMappings:
                    description: Available Area Configuration List. You must add at
                      least two zones. You can add a maximum of 10 zones. See zone_mappings
                      below.
                    items:
                      properties:
                        allocationId:
                          description: The ID of the elastic IP address (EIP) that
                            is associated with the Internet-facing NLB instance. You
                            can specify one EIP for each zone. You must add at least
                            two zones. You can add a maximum of 10 zones.
                          type: string
                        eniId:
                          description: The ID of the elastic network interface (ENI).
                          type: string
                        ipv4LocalAddresses:
                          description: IPv4 Local address list. The list of addresses
                            that NLB interacts with backend services.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        ipv6Address:
                          description: The IPv6 address of the NLB instance.
                          type: string
                        ipv6LocalAddresses:
                          description: IPv6 Local address list. The list of addresses
                            that NLB interacts with backend services.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        privateIpv4Address:
                          description: The private IP address. You must add at least
                            two zones. You can add a maximum of 10 zones.
                          type: string
                        publicIpv4Address:
                          description: The public IPv4 address of the NLB instance.
                          type: string
                        status:
                          description: The status of the NLB instance.
                          type: string
                        vswitchId:
                          description: The vSwitch in the zone. You can specify only
                            one vSwitch (subnet) in each zone of an NLB instance.
                            You must add at least two zones. You can add a maximum
                            of 10 zones.
                          type: string
                        zoneId:
                          description: |-
                            The ID of the zone of the NLB instance. You must add at least two zones. You can add a maximum of 10 zones.
                            You can call the DescribeZones operation to query the most recent zone list.
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
